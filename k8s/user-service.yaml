---
# User Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: feed-system
  labels:
    app: user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: distributed-feed/user-service:latest
          ports:
            - containerPort: 50051
          env:
            - name: DATABASE_URL
              valueFrom:
                configMapKeyRef:
                  name: feed-system-config
                  key: DATABASE_URL_USER
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: feed-system-config
                  key: KAFKA_BROKERS
            - name: GRPC_PORT
              valueFrom:
                configMapKeyRef:
                  name: feed-system-config
                  key: GRPC_PORT_USER
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: feed-system-secrets
                  key: JWT_SECRET
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          livenessProbe:
            exec:
              command:
                - node
                - -e
                - "require('net').createConnection(50051, 'localhost').on('connect', () => process.exit(0)).on('error', () => process.exit(1))"
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - node
                - -e
                - "require('net').createConnection(50051, 'localhost').on('connect', () => process.exit(0)).on('error', () => process.exit(1))"
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
---
# User Service - ClusterIP Service
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: feed-system
spec:
  selector:
    app: user-service
  ports:
    - port: 50051
      targetPort: 50051
  type: ClusterIP
---
# User Service - HorizontalPodAutoscaler
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: user-service-hpa
  namespace: feed-system
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
